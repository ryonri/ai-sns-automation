# Optional: server-side fallback Gemini key.
# By default this app uses BYOK (the user pastes their own key in the UI),
# so this is only needed if you want to provide a default key for everyone.
GOOGLE_GENERATIVE_AI_API_KEY=
